const {shuffleArray} = require('./utils')

let lameArr = [1, 2, 3, 4]

describe('shuffleArray should', () => {
     test('should return array', () => {
         let returnedArray =shuffleArray(lameArr)
         expect(dataArray.length).toEqual(lameArr.length)
     });
     test('should return array', () => {
        let returnedArray =shuffleArray(lameArr)
        expect(dataArray.length).toEqual(lameArr.length)
    })
     
})

